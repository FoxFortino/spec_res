This file contains dataset, noise and signal parquet files constructed such that the default noiseWindowBlu and noiseWindowRed parameter were set to 200 Angstroms (up from 100). Ended up not using this.

The closer_look file represents a subselection of data that was created when diagnosing various issues we were having with how the SNR algorithm worked.